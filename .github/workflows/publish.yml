name: publish

on:
  push:
    branches: [main]
    paths:
      - 'packages/**'
      - '.github/workflows/publish.yml'
  workflow_dispatch: {}

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # Trusted Publishing: npm exchanges the OIDC token (no NPM_TOKEN secret)
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate sources
        run: |
          set -e
          node --check packages/prompt/index.js
          node --check packages/fff/index.js
          node --check packages/tools/index.js
          node --check packages/runtime/index.js
          node --check packages/web/index.js
          node --check packages/tui/index.js
          DSH_PI_TUI_TEST=1 node packages/tui/test.js
          node packages/web/test.js

      - name: Publish changed packages
        run: |
          set -e
          for pkg in prompt fff tools runtime web tui dsh-pi; do
            name="@dsh-pi/$pkg"
            local="$(node -p "require('./packages/$pkg/package.json').version")"
            published="$(npm view "$name" version 2>/dev/null || echo none)"
            echo "::group::$name local=$local published=$published"
            if [ "$published" = "none" ] || [ "$local" != "$published" ]; then
              npm publish --workspace "$name" --provenance --access public
              echo "published $name@$local"
            else
              echo "skipping $name@$local (already published)"
            fi
            echo "::endgroup::"
          done
